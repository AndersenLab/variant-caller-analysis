threads = 4

rule addsnvs:
    input:
        "varsets/{num}.snps.txt"
    output:
        "bam/{num}.snps.bam"
    threads: 8
    shell:
        """
            bamsurgeon/addsnv.py --reference {reference} --procs {threads} --maxdepth 2000 -v {input}
        """

